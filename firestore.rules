rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each user owns one diagram document keyed by their UID
    match /diagrams/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
